# Brax PPO Hopper run using Brax PPO defaults.
# Only the environment, logging destination, and requested training steps are set.
logging: "aim"
log_repo: "logs/aim_ppo/.aim"
env_params:
  env_name: "brax-hopper"
num_timesteps: 2000000
