# Issue #8: Rerun Brax PPO baselines with provided hyperparameters

State at snapshot: `wip`

- Issue: https://github.com/le2333/RTRRL-AAAI25/issues/8
- User authorization: start executing #8; run HalfCheetah with the provided official PPO hyperparameters.
- Follow-up decision: the provided official mapping does not include Hopper PPO parameters; its Hopper entry uses `sac.train`, so Hopper was paused.
- Follow-up authorization: use the supplied cgpax PPO parameters to try Hopper now, and prepare the supplied cgpax HalfCheetah PPO parameters for a later rerun.
- Follow-up authorization: run masked POMDP tests with `env_params.obs_mask: even`, keeping even observation entries and discarding odd entries.

## Scope

- Use the official hyperparameter mapping provided in Issue #8.
- Run HalfCheetah with the official `ppo.train` settings.
- Run Hopper with the supplied cgpax `ppo.train` settings.
- Prepare, but do not yet start, the supplied cgpax HalfCheetah `ppo.train` settings.
- Run Hopper with the official `sac.train` settings when explicitly requested.
- Run masked POMDP variants:
  - HalfCheetah with official PPO settings plus `obs_mask: even`.
  - Hopper with official SAC settings plus `obs_mask: even`.
- Log all runs to the unified Aim repo: `logs/aim/.aim`.
- Record run hashes, completion state, and invalid attempts in Issue #8.

## Non-Goals

- Do not run the other official environments in this issue unless explicitly requested.
- Do not tune parameters beyond translating the official settings into project configs/entrypoints.
- Do not split Aim data into separate PPO/RTRRL/SAC repositories.

## Verification Checklist

- [ ] Config preserves the official HalfCheetah PPO values.
- [ ] Config preserves the cgpax Hopper PPO values.
- [ ] Config preserves the cgpax HalfCheetah PPO values.
- [ ] Config preserves the masked official HalfCheetah PPO values with `obs_mask: even`.
- [ ] Config preserves the masked official Hopper SAC values with `obs_mask: even`.
- [ ] Hopper runs through the cgpax PPO algorithm settings.
- [ ] HalfCheetah runs through the official PPO algorithm settings.
- [ ] Aim records environment names, hyperparameters, rewards, evals, and step axes.
- [ ] Completed run hashes and any failed/invalid attempts are archived in Issue #8.
