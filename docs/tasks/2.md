# Issue #2 Task Snapshot

Issue: [Add Aim-logged Brax PPO baseline entrypoint](https://github.com/le2333/RTRRL-AAAI25/issues/2)

Workflow state source: Issue labels only. This snapshot records the execution
scope and does not maintain local state.

## Confirmed Records

- Gate 1 superseding PPO baseline contract:
  <https://github.com/le2333/RTRRL-AAAI25/issues/2#issuecomment-4702636663>
- Gate 2 confirmed execution plan and authorization:
  <https://github.com/le2333/RTRRL-AAAI25/issues/2#issuecomment-4702914535>

## Scope

- Add formal `ppo_baseline.py` entrypoint for Brax PPO experiments.
- Align parameter parsing and logger wrapping style with `rtrrl.py`.
- Use Aim as the intended PPO experiment tracking path.
- Preserve Brax PPO defaults unless explicit `ppo_overrides` are supplied.
- Reuse existing `EnvironmentParams` conventions for Brax env setup and
  observation masks.
- Add minimal documentation for PPO/Aim usage and field alignment.

## Non-Goals

- No MLflow, Optuna, or sweep infrastructure.
- No default hand-written JSONL runtime logger for PPO.
- No W&B removal.
- No RTRRL behavior or logger option changes.
- No long experiments, hyperparameter tuning, or scientific conclusions.
- No deletion or migration of historical logs.
- No commit, push, PR, or Issue close without later explicit authorization.

## Verification Checklist

- [x] `ppo_baseline.py` exists and follows `rtrrl.py`-style dataclass parsing,
      YAML config loading, and `with_logger` wrapping.
- [x] PPO calls Brax PPO directly and does not re-declare unspecified Brax PPO
      defaults as wrapper defaults.
- [x] Observation masking is applied in the actual PPO environment.
- [x] Aim logging records `train/env_steps` as the main comparison axis and
      includes key PPO/shared fields.
- [x] PPO does not write default runtime JSONL records.
- [x] Minimal docs explain PPO usage, Aim logging, and PPO/RTRRL field mapping.
- [x] Smoke/lint checks are completed or limitations documented.
- [x] Staged delivery excludes unrelated old configs and runtime artifacts.
- [x] Final reviewer/code review has checked the frozen staged diff.

## Review Handoff Notes

Implementation completed. Checks run:

- `poetry run python -m py_compile ppo_baseline.py`
- `poetry run python ppo_baseline.py --help`
- short DummyLogger PPO smoke run
- masked observation-size check
- short Aim PPO smoke run using `config/ppo_smoke.yml`
- `poetry check`
- actual PPO env backend check
- Aim 3.28.0 persistence/query check
- fixed-step 2M default Hopper PPO run using
  `config/ppo_hopper_default_2m.yml`
- AimLogger step-axis fix (`step` instead of `epoch`)
- ReadLints on edited files

Aim 3.29.1 created run metadata but did not make metrics queryable in this
environment. The dependency is pinned to Aim 3.28.0, where metrics and summary
values were verified to persist and query correctly.

After user rejection, Aim visibility was fixed and verified with run
`416d048593e94a1a8407e214`: `eval/episode_reward` and `train/env_steps` are
queryable at step `2001920`.

Final reviewer verdict after visibility fix: pass-with-risks. No blocking
issues found. Residual risks to disclose: Aim's dependency tree is large, and
smoke/full-default verification was done on the local Python 3.10 Poetry
environment.

## Acceptance Handoff Notes

Prepared in chat. The accepted staged delivery should include only Issue #2
files and exclude old untracked configs and runtime artifacts.
