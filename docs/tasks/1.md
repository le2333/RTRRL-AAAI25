# Issue #1 Task Snapshot

Issue: [Refactor repository for reproducible PPO/RTRRL experiment workflow](https://github.com/le2333/RTRRL-AAAI25/issues/1)

Workflow state source: Issue labels only. This snapshot does not maintain local
state.

## Confirmed Records

- Gate 1 confirmed requirement contract:
  <https://github.com/le2333/RTRRL-AAAI25/issues/1#issuecomment-4700392401>
- Gate 2 confirmed execution plan and authorization:
  <https://github.com/le2333/RTRRL-AAAI25/issues/1#issuecomment-4700408533>

## Scope

- Create minimal project workflow documentation under `docs/`.
- Document the role and timing of `docs/tasks/<issue>.md` snapshots.
- Update project hygiene configuration only where needed to avoid common local
  non-versioned files polluting git status.
- Keep Issue #1 focused on workflow/documentation infrastructure.

## Non-Goals

- No experiment management, metrics schema, plotting workflow, MLflow, run
  artifact design, or experiment directory schema.
- No PPO/RTRRL algorithm behavior changes.
- No new dependencies.
- No training experiments.
- No commit, push, PR, or Issue close without later explicit authorization.

## Authorized Plan

1. Create `docs/workflow.md`.
2. Create this `docs/tasks/1.md` task snapshot after Gate 2 archival and before
   moving the Issue label to `wip`.
3. Update `.gitignore` minimally for generic local caches/temp files and
   existing local non-versioned log outputs.
4. Avoid `.github/ISSUE_TEMPLATE` unless documentation alone is insufficient.
5. Verify only Issue #1 scoped files are staged before acceptance.

## Verification Checklist

- [x] `docs/workflow.md` covers gates, lifecycle states, acceptance, close-out,
      and task snapshots.
- [x] `.gitignore` does not ignore `docs/`, `config/`, `scripts/`, or source
      files.
- [x] No executable PPO/RTRRL behavior changed.
- [x] Only Issue #1 scoped files are staged for review.
- [x] Reviewer has checked the completed work and acceptance handoff.

## Review Handoff Notes

Reviewer verdict: pass-with-risks.

The staged delivery is within Issue #1 scope. The remaining risk is that
`logs/` is intentionally broad for local run outputs; future curated experiment
artifacts should be defined by a separate experiment-management Issue before
being versioned.

## Acceptance Handoff Notes

Prepared in chat. The accepted delivery state should be the staged Issue #1
files only: `.gitignore`, `docs/workflow.md`, and `docs/tasks/1.md`.
